<%= bootstrap_form_for([@user, @category, @gallery]) do |f| %>
  <%= render 'shared/panel_body', f: f, form: 'galleries/form' %>
  <%= render 'shared/panel_footer', f: f, back_link: @back_link %>
<% end %>